<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppSimulator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/shopify_color</item>
        <item name="colorPrimaryVariant">@color/shopify_color</item>
        <item name="colorOnPrimary">@color/shopify_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/shopify_color</item>
        <item name="colorSecondaryVariant">@color/shopify_color</item>
        <item name="colorOnSecondary">@color/shopify_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>